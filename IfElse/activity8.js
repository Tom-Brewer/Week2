// Activity 8

let word = "Brew";
if (word.charAt(0) == word.charAt(word.length-1)){
  console.log("The last letter is the same as the first")
} else {
  console.log("The last letter is different to the first")
}